// middlewares entre la ruta y el controlador
// funciones encadenadas uno despues del otro que haga que la info llegue de la manera correcta que se usan dependiendo de la ruta
// asegurarnos que la ruta sea exactamente la que queramos
//  en los middlewares se pueden validar, formatear la info
// validar autentificacion
// netx- es un callback para cambiar al siguiente middlewares
// TODOs en la misma cadena comparten el mismo req y res,
// TODO se tiene que registar

// joi
// crear schema
// schema para validar